# Example Docs

This is a placeholder doc file. Its only job right now is to give our
`docs-map.json` something real to point at, so we can test the mapping
logic before we build anything fancy.

## The greet() function

`hello.py` has a function called `greet(name)` that returns the string
`"Hello, {name}!"`.
